source "https://rubygems.org"

gem "jekyll", "~> 4.4.1"
gem "jekyll-bear-theme"

# Ruby 3.4 unbundled erb from core and the distro's system Ruby doesn't ship
# it, so it has to be requested explicitly.
gem "erb"

group :jekyll_plugins do
  gem "jekyll-feed", "~> 0.12"
end
